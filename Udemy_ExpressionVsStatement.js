//Anything that produces a value is a expression, if does not it is a Statement

4 + 8
1991 
true && false && !false
// all exmples above produces value hence it's a expression

if (10 > 67) {
    const str = "10 is larger";
}

// Above if does not produce a value hence it's a statement
